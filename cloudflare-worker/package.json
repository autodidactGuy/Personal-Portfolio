{
  "name": "portfolio-decap-auth-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "start": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.9.1"
  },
  "dependencies": {
    "hono": "^4.12.14",
    "zod": "^4.3.6"
  }
}
